<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.redowsko">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".application.ui.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".application.ui.HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme" />
        <activity android:name=".application.ui.SelectBabActivity" />
        <activity android:name=".application.ui.SelectAreaActivity" />
        <activity android:name=".application.ui.QuestionActivity" />
        <activity android:name=".application.ui.LoginActivity" />
        <activity android:name=".application.ui.SelectSurveriorActivity" />
        <activity android:name=".application.ui.SelectHospitalActivity" />
        <activity android:name=".application.ui.SurveriorTaskActivity"></activity>
    </application>

</manifest>